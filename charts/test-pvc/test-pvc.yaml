apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-gp2-pvc
  namespace: emcp-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: gp2